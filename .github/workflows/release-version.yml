name: Release Version

on:
  workflow_dispatch:

jobs:
  release-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Release Version
        run: |
          node scripts/version-updater.js
      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v5

